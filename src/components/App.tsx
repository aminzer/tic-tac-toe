import React from 'react';
import AppWrapper from './AppWrapper';
import Game from './Game';

const App: React.FC = () => {
  return (
    <AppWrapper>
      <Game />
    </AppWrapper>
  );
};

export default App;
