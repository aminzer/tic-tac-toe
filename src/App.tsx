import React from 'react';
import AppWrapper from './AppWrapper';
import Routes from './routes';

const App: React.FC = () => {
  return (
    <AppWrapper>
      <Routes />
    </AppWrapper>
  );
};

export default App;
